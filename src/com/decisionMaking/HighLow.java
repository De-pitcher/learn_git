package com.decisionMaking;

public class HighLow {
   /* public static void main(String[] args) {

    }
    /**
     * Lets the user play one game of HighLow, and returns the
     * user's score int hat game. The score is number of
     * correct guesses that the user makes.
     */
   /* private static int play(){
        Deck deck=new Deck();  //Get a new deck of cards, and store
                                // a reference to it in the variable deck

        Cards currentCard;      // The current card which the user sees.

        Cards nextCard;         // The next card in the deck.
                                //The user tries to predict whether it is higher or lower
                                // than the current card.

        int correctGuesses;     //The number of correct predictions the
                                //user has made. At the end of the game
                                // this will be the user's score.

        char guess;         //The user's guess. 'H' if the user predicts that
                            //the next card will be higher, 'L' if the user predicts that
                            //it will be lower.
        deck.shuffle();     // Shuffle the deck into a random order before
                            // starting the game.

        correctGuesses=0;
        currentCard=deck.dealCard();
        System.out.println("The first card is the "+currentCard);

        while (true){   //Loops end when the user's prediction is wrong.
            /* Get the user's prediction, 'H' or 'L' (or 'h' or 'l').*/
           /* System.out.println("Will the next card be higher(H) or lower(L)? ");

            }
    }*/
}
